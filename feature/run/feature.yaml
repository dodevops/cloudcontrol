title: "👟 Run: Run commands when entering cloud control"
description: "Runs commands inside the shell when entering the cloud control container"
configuration:
    - "Environment RUN_COMMANDS: Valid shell commands to run"
